<?xml version="1.0"?>
<!--
  VERBATIM COPY. DO NOT EDIT THIS FILE HERE.

  Apart from this comment block, this file is copied byte for byte from

      data/org.freedesktop.impl.portal.Request.xml

  in xdg-desktop-portal itself (branch experimental/integration,
  the file is unchanged from upstream main 86bd3e2). It is the shared
  backend-side Request interface every portal backend exports, and it is
  reproduced here only because no released xdg-desktop-portal installs the impl
  interface XML where an out-of-tree backend can codegen from it. See
  docs/IMPL-INTERFACE.md.

  The file is LGPL-2.1-or-later, and so is this repository; see
  docs/decisions/0004-license.md.
-->
<!--
 SPDX-License-Identifier: LGPL-2.1-or-later
 SPDX-FileCopyrightText: Copyright © the xdg-desktop-portal contributors
-->

<node name="/" xmlns:doc="http://www.freedesktop.org/dbus/1.0/doc.dtd">
  <!--
      org.freedesktop.impl.portal.Request:
      @short_description: Shared request interface

      The Request interface is shared by all portal backend interfaces.
      When a backend method is called, the backend exports a Request object
      on the object path that was sent with the method call. The Request
      will stay alive for the duration of the user interaction related to
      the method call.

      The portal can abort the interaction by calling
      :ref:`org.freedesktop.impl.portal.Request.Close` on the Request object.
  -->
  <interface name="org.freedesktop.impl.portal.Request">

    <!--
        Close:

        Ends the user interaction to which this object refers.
        Dialogs and other UIs related to the request should be closed.
    -->
    <method name="Close">
    </method>
  </interface>
</node>
